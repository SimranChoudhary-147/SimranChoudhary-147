class Solution
{
    public:
    //Function to count nodes of a linked list.
    int getCount(struct Node* head){
        int cnt=1;
        struct Node* temp;
        temp=head;
       while(temp->next != NULL)
       {
           cnt++;
           temp=temp->next;
       }
       return cnt;
    
    }
};
