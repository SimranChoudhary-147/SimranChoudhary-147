class Solution{   
public:
    int shortestPath( int x, int y){ 
       int len=0;
      while(x!=y)
      {
          if(x>y)
            x=(x/2);
          else
            y=(y/2);
          len+=1;
      }
       return len;
    }
};
