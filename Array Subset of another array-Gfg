tring isSubset(int a1[], int a2[], int n, int m) {
    multiset<int>s;
    for(int i=0;i<n;i++) s.insert(a1[i]);
    for(int i=0;i<m;i++)
    {
        if(s.find(a2[i])==s.end())
        {
            return "No";
        }
        else
        {
            s.erase(s.find(a2[i]));
        }
    }
    return "Yes";
    
}
